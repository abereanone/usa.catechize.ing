node_modules/
dist/
.astro/

# Build artifacts
src/generated/
public/assets/search-index.json
package-lock.json

# Authored question content. Prettier reformats Markdown (list markers, wrapping,
# escaping), which can change what renders. Content is edited by hand, not formatted.
src/content/

# One-time import source text, not code.
scripts/patriots_catechism_extracted.txt

# prettier-plugin-astro 0.14.1 cannot parse a <script> nested inside a template
# expression (verified with a minimal repro: top-level scripts parse fine, nested
# ones throw). Both files below need that shape on purpose:
#   Main.astro          - analytics block is conditional on the GA id
#   QuestionAnswer.astro - toggle script must not ship when the toggle is unused
src/layouts/Main.astro
src/components/QuestionAnswer.astro
