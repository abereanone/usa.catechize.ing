---
import Main from "@/layouts/Main.astro";
import { siteSettings } from "@/config/siteSettings";
import { listAuthors } from "@/lib/questions";

const showQuestionIds = siteSettings.showQuestionId;
const searchClientSrc = `/assets/search-client.js?v=${siteSettings.version}`;
const searchIndexUrl = `/assets/search-index.json?v=${siteSettings.version}`;
const siteName = siteSettings.branding.siteName;
const siteDescription = siteSettings.branding.description;
const authors = listAuthors().filter((author) => author.count > 0);
const authorOptions = authors.map((author) => ({ id: author.id, name: author.name }));
---

<Main title={`Search | ${siteName}`} description={`Search across ${siteName} questions and answers.`}>
  <section
    class="search-page"
    data-search-root
    data-show-question-ids={showQuestionIds ? "true" : "false"}
    data-has-authors={authors.length ? "true" : "false"}
    data-author-map={JSON.stringify(authorOptions)}
    data-search-index-url={searchIndexUrl}
  >
    <header class="search-header">
      <h1>Search</h1>
      <p>Find questions, answers, and related resources across {siteName}.</p>
    </header>

    <form id="search-form" role="search" class="search-form">
      <label class="sr-only" for="search-input">Search questions</label>
      <input
        id="search-input"
        type="search"
        name="q"
        placeholder="Search questions or topics..."
        autocomplete="off"
        spellcheck="false"
      />
      <button type="submit">Search</button>
    </form>

    {authors.length ? (
      <div class="author-filter" data-author-filter>
        <button type="button" class="author-pill is-active" data-author="">All authors</button>
        {authors.map((author) => (
          <button type="button" class="author-pill" data-author={author.id}>
            {author.name}
          </button>
        ))}
      </div>
    ) : null}

    <div id="search-meta" class="search-meta" aria-live="polite">Type a phrase to begin searching.</div>
    <div id="search-results" class="search-results" aria-live="polite"></div>
  </section>

  <script type="module" src={searchClientSrc}></script>

  <style>
    .search-page {
      max-width: 840px;
      margin: 0 auto;
      padding: 2.5rem 1.25rem 4rem;
    }

    .search-header h1 {
      font-size: 2.5rem;
      margin-bottom: 0.25rem;
    }

    .search-header p {
      color: var(--secondary-text);
      margin: 0 0 1.5rem;
    }

    .search-form {
      display: flex;
      gap: 0.75rem;
      margin-bottom: 1rem;
    }

    .search-form input[type="search"] {
      flex: 1;
      padding: 0.85rem 1rem;
      font-size: 1.1rem;
      border-radius: 0.75rem;
      border: 1px solid var(--input-border);
      background: var(--input-bg);
      color: var(--input-text);
    }

    .search-form input[type="search"]::placeholder {
      color: var(--input-placeholder);
    }

    .search-form button {
      padding: 0.85rem 1.5rem;
      border-radius: 0.75rem;
      border: none;
      background: var(--accent-bg);
      color: var(--accent-text);
      font-weight: 600;
      cursor: pointer;
      transition: background-color 0.2s ease;
    }

    .search-form button:hover {
      background: var(--accent-bg-hover);
    }

    .search-meta {
      margin-bottom: 1.25rem;
      color: var(--meta-text);
    }

    .author-filter {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      margin-bottom: 1rem;
    }

    .author-pill {
      border: 1px solid var(--surface-border);
      background: transparent;
      color: var(--secondary-text);
      padding: 0.35rem 0.9rem;
      border-radius: 999px;
      font-size: 0.95rem;
      cursor: pointer;
      transition: background 0.2s ease, border-color 0.2s ease, color 0.2s ease;
    }

    .author-pill.is-active {
      background: var(--surface-bg-strong);
      border-color: var(--surface-border-strong);
      color: var(--color-text);
    }

    .search-results {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }

    .search-result {
      padding: 1.25rem;
      border-radius: 1rem;
      background: var(--surface-result-bg);
      border: 1px solid var(--surface-result-border);
    }

    .search-result h2 {
      margin: 0 0 0.35rem;
      font-size: 1.5rem;
    }

    .search-result a {
      color: var(--color-text);
      text-decoration: none;
    }

    .search-result a:hover {
      text-decoration: underline;
    }

    .result-snippet {
      margin: 0;
      color: var(--secondary-text);
      line-height: 1.6;
    }

    .result-snippet mark {
      background: rgba(255, 215, 0, 0.2);
      padding: 0 0.15rem;
      border-radius: 0.2rem;
    }

    .result-id {
      display: inline-block;
      margin-right: 0.4rem;
      color: var(--highlight-text);
      font-size: 0.95rem;
      font-weight: 600;
    }

    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }

    @media (max-width: 640px) {
      .search-form {
        flex-direction: column;
      }
      .search-form button {
        width: 100%;
      }
    }
  </style>
</Main>
